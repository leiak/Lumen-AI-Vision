[project]
name = "visual-recognition-backend"
version = "0.1.0"
description = "Backend service for abnormal vehicle stay recognition"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.7.0",
  "pydantic-settings>=2.3.0",
  "SQLAlchemy>=2.0.30",
  "alembic>=1.13.0",
  "psycopg[binary]>=3.1.0",
  "redis>=5.0.0",
  "celery>=5.4.0",
  "minio>=7.2.0",
  "python-multipart>=0.0.9",
  "PyJWT>=2.8.0",
  "bcrypt>=4.1.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.2.0",
  "httpx>=0.27.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
